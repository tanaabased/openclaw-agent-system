schema-version: 1

agent:
  id: tanaabot
  name: Tanaabot Containment Source
  email: tanaabot-containment@example.invalid

git: {}

setup:
  check: |
    printf 'checked\n' >> "$TMPDIR/agent-system-doctor-check"
    test -f "$TMPDIR/agent-system-forbidden-setup"
  apply: touch "$TMPDIR/agent-system-forbidden-setup"

schema-version: 1

agent:
  id: tanaabot
  name: Tanaabot
  email: tanaabot@tanaab.dev

environment:
  op: jglytdfegfggijqkalco2cxexa
  set:
    SSH_KEY: $TEST_SSH_KEY

git:
  extensions:
    agent-system-test: allow
  ssh:
    private-keys:
      from-environment: SSH_KEY

github:
  username: tanaabot
  token: GH_TOKEN_TANAABOT
  policy:
    admin: allow
    destructive: allow

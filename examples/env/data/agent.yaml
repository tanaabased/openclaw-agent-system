schema-version: 1

agent:
  id: data
  name: Data
  description: Environment delivery test agent.
  avatar: avatar.png

environment:
  set:
    AGENT_SYSTEM_LEIA_VISIBLE: leia-agent-system-visible
    GITHUB_TOKEN: leia-agent-system-filtered

schema-version: 1

agent:
  id: emori
  name: EMORI
  email: emori@tanaab.dev

environment:
  set:
    GH_TOKEN_EMORI: ${GH_TOKEN_EMORI}
  required:
    - GH_TOKEN_EMORI

github:
  host: github.com
  username: emoriwan
  token: GH_TOKEN_EMORI

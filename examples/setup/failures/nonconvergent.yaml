schema-version: 1
agent:
  id: setup-failures
setup:
  steps:
    - id: nonconvergent
      check: [test, -f, never-created]
      apply: touch nonconvergent-applied
    - id: forbidden
      apply: touch forbidden-later

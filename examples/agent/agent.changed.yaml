schema-version: 1

agent:
  id: data
  name: Data
  description: leia-changed-manifest-value
  avatar: avatar.png

environment:
  set:
    AGENT_SYSTEM_LEIA_EXEC: $AGENT_SYSTEM_LEIA_SOURCE
  required:
    - AGENT_SYSTEM_LEIA_EXEC

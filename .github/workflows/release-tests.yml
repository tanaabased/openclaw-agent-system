name: Release tests

on:
  pull_request:

permissions:
  contents: read

env:
  STAMPED_VERSION: v9.9.9-ci.${{ github.event.pull_request.head.sha }}
  STAMPED_DATE: '2026-01-01T00:00:00Z'
  STAMPED_URL: https://github.com/${{ github.repository }}/releases/tag/v9.9.9-ci.${{ github.event.pull_request.head.sha }}

jobs:
  release-tests:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Install Bun
        uses: tanaabased/actions/setup-bun@v1
      - name: Install Node
        uses: tanaabased/actions/setup-node@v1
      - name: Install dependencies
        run: bun install --frozen-lockfile --ignore-scripts
      - name: Prepare synthetic release
        uses: tanaabased/actions/prepare-release@v1
        with:
          version: ${{ env.STAMPED_VERSION }}
          release-date: ${{ env.STAMPED_DATE }}
          release-url: ${{ env.STAMPED_URL }}
          commands: |
            version-injector openclaw.plugin.json --style json --version "$PREPARE_RELEASE_VERSION"
            bun run format:write
      - name: Validate prepared package
        run: |
          bun run build
          bun run plugin:check
          bun run format:check
      - name: Pack prepared package
        id: pack
        uses: tanaabased/actions/npm-pack@v1
      - name: Run release package tests
        env:
          AGENT_SYSTEM_PACKAGE: ${{ steps.pack.outputs.tarball-path }}
        run: bun run test:release
      - name: Validate npm publisher
        uses: tanaabased/actions/publish-npm@v1
        with:
          dry-run: true
          tarball: ${{ steps.pack.outputs.tarball-path }}
          update-prerelease-tag-on-stable: true
      - name: Validate ClawHub publisher
        uses: tanaabased/actions/publish-clawhub@v1
        with:
          dry-run: true
          tarball: ${{ steps.pack.outputs.tarball-path }}
          owner: tanaab
          tags: edge
          source-repo: ${{ github.repository }}
          source-commit: ${{ github.event.pull_request.head.sha }}
          wait-timeout: 1800

name: PR notification tests

on:
  pull_request:

permissions:
  contents: read

jobs:
  notifications:
    strategy:
      fail-fast: false
      matrix:
        scenario:
          - assignment
          - assignment-provider-proof
    uses: ./.github/workflows/reusable-notification-test.yml
    with:
      provider: mock
      runner: ubuntu-24.04
      scenario: ${{ matrix.scenario }}
    secrets:
      op_service_account_token: ${{ secrets.TANAAB_OP_TESTVAULT }}

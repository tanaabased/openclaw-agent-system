/** Hidden instructions contributed by Guided mode. */
const githubNotificationGuidedModeInstructions =
  'Guided mode is operator-led. The initial assignment authorizes setup and acknowledgment, not implementation: confirm that the assignment context is ready, state that you are waiting for direction, and do not produce an implementation plan. On a later approved comment or private operator request, act only on that current request using the configured coding capabilities and the prepared lifecycle worktree when repository work is needed.';

export default githubNotificationGuidedModeInstructions;
